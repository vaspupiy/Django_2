1. Стоит ли добавлять индекс 
в
class Order(models.Model):
...
status = models.CharField(choices=ORDER_STATUSES, default=FORMING, verbose_name='статус', max_length=3)
?(при каждом изменении статуса происходит переиндексация базы?)

2. В методичке пишут:
Следует помнить, что индексы автоматически добавляются Django для всех полей «ForeignKey».
а на лекции, мы выяснили, что все-таки не создает... Опечатка в методичке?
